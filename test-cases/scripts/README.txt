# Readme file for running testrepot.py

This script generates test report per lab and meta files per experiment from org format.

Prerequisite:

$ pip install texttable

To run the script execute it as follows:

$ python <testreport.py path> <lab path>

Destination of the testreport files will be the same as lab path.
Destination of the metafiles will inside each experiment.

